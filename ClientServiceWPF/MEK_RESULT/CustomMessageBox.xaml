<Window x:Class="ClientServiceWPF.MEK_RESULT.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientServiceWPF.MEK_RESULT"
        mc:Ignorable="d"
        Title="CustomMessageBox" Height="149" Width="427.051" Style="{DynamicResource StyleWindow}" WindowStartupLocation="CenterScreen" >
    <Grid>
        <Button x:Name="buttonYes" Content="Да" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="132" Style="{DynamicResource MainButton}" Click="buttonYes_Click"/>
        <Button x:Name="buttonNo" Content="Нет" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="132" Style="{DynamicResource MainButton}" Click="buttonNo_Click"/>
        <TextBox x:Name="textBoxText" Margin="10,0,10,25" TextWrapping="Wrap" Text="asd" BorderThickness="0" Background="Transparent" IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
